FROM node:18-alpine

WORKDIR /app

# Copy just package files
COPY package*.json ./

# Use npm install instead of npm ci to auto-generate lock file if missing
RUN npm install --legacy-peer-deps

# Copy app
COPY . .

EXPOSE 5173

CMD ["npm", "run", "dev"]