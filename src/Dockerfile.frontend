FROM node:18-alpine

ARG FRONTEND_DIR="Web Interface for AURA Clinic (1)"

WORKDIR /app

# Copy frontend files
COPY "$FRONTEND_DIR/package*.json" ./

RUN npm ci --legacy-peer-deps

COPY "$FRONTEND_DIR/" .

EXPOSE 5173

CMD ["npm", "run", "dev"]
